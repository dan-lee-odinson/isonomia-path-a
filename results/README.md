# results/

Per-run outputs land here. Per-epoch CSV/JSON logs are **not** committed (see `.gitignore`);
summary reports (`summary*.md`, `summary*.json`, scenario and sweep reports) are.

Layout per run: `results/<run_name>/epochs.csv`, `events.jsonl`, `summary.json`, `summary.md`.
